import assert from 'node:assert/strict';
import {readFile,stat,mkdir} from 'node:fs/promises';import{createServer}from'node:http';import path from'node:path';import{chromium}from'playwright';
const routes=['/','/product/','/use-cases/','/waitlist/','/pilot/','/privacy/','/terms/','/disclosure/','/waitlist/thanks/'];const mime={'.css':'text/css; charset=utf-8','.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.png':'image/png','.svg':'image/svg+xml','.txt':'text/plain; charset=utf-8','.xml':'application/xml; charset=utf-8'};let server;let base=process.env.BASE_URL;
if(!base){const root=process.cwd();server=createServer(async(req,res)=>{try{const pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=path.resolve(root,`.${pathname}`);if(!file.startsWith(`${root}${path.sep}`)&&file!==root)throw new Error('invalid path');if((await stat(file)).isDirectory())file=path.join(file,'index.html');const body=await readFile(file);res.writeHead(200,{'Content-Type':mime[path.extname(file)]||'application/octet-stream'});res.end(body);}catch{res.writeHead(404,{'Content-Type':'text/plain'});res.end('Not found');}});await new Promise((resolve,reject)=>{server.once('error',reject);server.listen(0,'127.0.0.1',resolve)});base=`http://127.0.0.1:${server.address().port}`;}base=base.replace(/\/$/,'');await mkdir('docs/screenshots',{recursive:true});const browser=await chromium.launch({headless:true});
try{for(const viewport of [{width:1440,height:900,label:'desktop'},{width:390,height:844,label:'mobile'}]){const page=await browser.newPage({viewport});const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});page.on('pageerror',e=>errors.push(e.message));for(const route of routes){const response=await page.goto(base+route,{waitUntil:'networkidle'});assert.equal(response.status(),200,`${route} 200`);assert.equal(await page.locator('h1').count(),1,`${route} one h1`);const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);assert.ok(overflow<=1,`${route} horizontal overflow ${overflow}px at ${viewport.width}`);}assert.deepEqual(errors,[],`console errors ${viewport.width}: ${errors.join('; ')}`);await page.goto(`${base}/`,{waitUntil:'networkidle'});await page.screenshot({path:`docs/screenshots/home-${viewport.label}.png`,fullPage:true});if(viewport.width<500){await page.locator('[data-nav-toggle]').click();assert.equal(await page.locator('[data-nav-toggle]').getAttribute('aria-expanded'),'true');assert.ok((await page.locator('[data-nav-menu]').getAttribute('class')).includes('open'));}await page.close();}
const wait=await browser.newPage();await wait.goto(`${base}/waitlist/`);for(const id of ['name','email','team','workflow','privacy-consent'])assert.ok(await wait.locator(`#${id}`).count(),`missing ${id}`);assert.equal(await wait.locator('input[name="source"]').getAttribute('value'),'gemsieve');assert.equal(await wait.locator('input[name="offer"]').getAttribute('value'),'early-access');await wait.close();const pilot=await browser.newPage();await pilot.goto(`${base}/pilot/`);for(const id of ['contact-name','contact-email','organization','brief','pilot-consent'])assert.ok(await pilot.locator(`#${id}`).count(),`missing ${id}`);assert.equal(await pilot.locator('input[name="offer"]').getAttribute('value'),'private-pilot');await pilot.close();}finally{await browser.close();if(server)await new Promise(r=>server.close(r));}console.log(`Verified ${routes.length} routes at desktop/mobile, menu, forms, overflow, and zero console errors.`);
